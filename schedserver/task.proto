syntax = "proto3";
package schedserver;

import "schedapi/api.proto";

// Internal representation of a task
// Stored in etcd
message Task {
    schedapi.TaskRequest request = 1;
    schedapi.TaskStatus status = 2;
    schedapi.TaskID id = 3;
}
